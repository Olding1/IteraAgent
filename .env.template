# Agent Zero v6.0 - API Configuration
# ==========================================

# Builder API Configuration (用于构建 Agent 时的推理)
# 推荐使用强模型以获得更好的代码生成质量
BUILDER_PROVIDER=
BUILDER_MODEL=
BUILDER_API_KEY=
BUILDER_BASE_URL=
BUILDER_TIMEOUT=60
BUILDER_MAX_RETRIES=3
BUILDER_TEMPERATURE=0.7

# Runtime API Configuration (用于生成的 Agent 运行时)
# 可以使用相同或更经济的模型
RUNTIME_PROVIDER=
RUNTIME_MODEL=
RUNTIME_API_KEY=
RUNTIME_BASE_URL=
RUNTIME_TIMEOUT=30
RUNTIME_TEMPERATURE=0.7

# Embedding API Configuration (用于 RAG 向量化)
# 使用本地 Ollama 的嵌入模型，免费且快速
EMBEDDING_PROVIDER=
EMBEDDING_MODEL=
EMBEDDING_BASE_URL=
# EMBEDDING_API_KEY 不需要（Ollama 本地运行）


# Judge API Configuration (用于 DeepEval 评测)
JUDGE_PROVIDER=
JUDGE_MODEL=
JUDGE_API_KEY=
JUDGE_BASE_URL=
JUDGE_TIMEOUT=60
JUDGE_TEMPERATURE=0.0